// Minimal PWA service worker – NO caching, NO cleanup, NO precache.
// This keeps IndexedDB stable for Firebase Auth.

self.addEventListener("install", () => {

});

self.addEventListener("activate", () => {

});

self.addEventListener("fetch", () => {

});
